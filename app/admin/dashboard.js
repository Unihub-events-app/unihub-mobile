import { RoutePlaceholder } from "../../components/RoutePlaceholder";

export default function AdminDashboardScreen() {
  return (
    <RoutePlaceholder
      title="Admin Dashboard"
      description="Admin routes are scaffolded so the dashboard, reports, payouts, and event management screens can be ported one by one."
    />
  );
}
